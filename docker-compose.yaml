# Gold Prediction System
#
# For local testing (all services on one machine):
#   docker compose -f docker-compose.local.yaml up -d
#
# For distributed deployment via Tailscale (per person):
#   docker compose -f docker-compose-person1.yaml up -d   # Yoeke (Data Ingestion)
#   docker compose -f docker-compose-person2.yaml up -d   # Dio (Stream Processing)
#   docker compose -f docker-compose-person3.yaml up -d   # Fatih (ML & MLOps)
#   docker compose -f docker-compose-person4.yaml up -d   # Angel (Serving & Monitoring)
